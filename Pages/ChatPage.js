import React from "react";
import { View, Text, SafeAreaView } from "react-native";

const ChatPage = () => {
    return (
        <SafeAreaView>
            <Text>Chat Page</Text>
        </SafeAreaView>
    );
};

export default ChatPage;